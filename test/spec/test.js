/* global describe, it */

(function () {
  'use strict';

  describe('Next shuttle calculator', function () {
      it('should convert to military time', function () {
        /*var now = new Date(2014, 9, 24, 4, 16),
        minutes = now.getMinutes(),
        hours = now.getHours().toString();
        
        if (minutes < 10) {
            minutes = '0' + minutes;
        } else {
            minutes = minutes.toString();
        }

        assert(hours + minutes) === '0416';
        */
      });
  });
})();
